{
  "name": "rentalify-aggregator",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "description": "Scrapes Facebook groups via Apify, cleans posts into normalized rental listings with the Claude Code CLI, geocodes them, and publishes a static map frontend.",
  "scripts": {
    "start": "node normalize.js",
    "publish": "node publish.mjs",
    "validate": "node validate.mjs",
    "eval": "node eval/run-eval.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1075.0",
    "apify-client": "^2.9.0"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1"
  }
}
