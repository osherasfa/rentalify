node_modules/
web/node_modules/
web/dist/
.tmp/
*.log
.DS_Store

# Per-run pipeline output. The durable progress lives in out/extract-cache.json
# (tracked) + state.json + listings-db.json; these per-run files are disposable
# once publish.mjs has merged them into the master DB.
out/listings-*.json
out/unmatched-*.json

# Eval artifacts: gold.jsonl + the scripts are tracked; the prediction cache and
# the last report are disposable (regenerated by run-eval.mjs).
eval/predictions.json
eval/last-report.json
