// ─────────────────────────────────────────────────────────────────────────────
// GovMap address search — paste your token here once approved.
//
//   1. Register at https://api.govmap.gov.il/  (domain: osherasfa.github.io)
//   2. Paste the token below, commit, and push — the deploy publishes it.
//
// The token is DOMAIN-LOCKED to osherasfa.github.io, so it only works on your
// live site and is safe to commit. While this is empty (""), the search bar
// falls back to the bundled offline gazetteer (town/neighborhood level).
//
// NOTE: GovMap won't work on http://localhost (the token is locked to the
// github.io domain). Test the live address search on the deployed site, or use
// the hosts-file alias for osherasfa.github.io.
// ─────────────────────────────────────────────────────────────────────────────
export const GOVMAP_TOKEN = "";
